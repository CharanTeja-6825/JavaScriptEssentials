// Arrays in Javasacript : we can fit multiple items in an array of different types in 
// javascript and we can manipulate its values by accessing the array using the index
// numbers..

// arr = ['Charan', 'Teja']
// index     0,       1,      2,........

const array =[5, 'Charan', 78.90];

// accessing and manipulating the array by accessing the array element using the index 
// number

array[2] = 'Teja';

// logging the modified array

console.log(array);